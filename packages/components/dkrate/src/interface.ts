export type { RateProps } from './props'

/**
 * Interface of the callback type triggered when the change occurs
 * @param { number } value new Value
 */
export type RateChange = (value: number) => number
